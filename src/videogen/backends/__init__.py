"""Render backends (ADR 0002): backends interpret the IR's 3 layer kinds, not per-overlay code.

base.py reserves the RenderBackend protocol; the only Remotion-specific code lives under remotion/.
"""
