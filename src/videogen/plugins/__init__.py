"""Registry-extensible plugins (ADR 0001/0002): layouts, overlays, captions.

Each future layout/overlay type lives in its own folder holding contract.py + ir.py
(the pure to_ir facet). No plugin is implemented in Phase 0 -- only the folders exist.
"""
